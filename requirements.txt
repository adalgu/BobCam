# BobCam 프로젝트 의존성 목록

# 기본 의존성 (기존)
opencv-python>=4.5.0
mediapipe>=0.8.0
numpy>=1.20.0
pillow>=8.0.0
pyttsx3>=2.90

# 새로운 의존성 (고급 감지용)
scipy>=1.7.0

# 개발용 의존성
ultralytics>=8.0.0  # YOLO (Phase 2에서 사용 예정)
matplotlib>=3.3.0   # 시각화 및 디버깅용

# 선택적 의존성
# tensorflow>=2.8.0  # 향후 딥러닝 모델 사용 시
# torch>=1.10.0      # PyTorch 기반 모델 사용 시
