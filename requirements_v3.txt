# BobCam v3.0 통합 시스템 필요 패키지

# 기존 패키지 (Phase 1)
opencv-python==4.8.1.78
mediapipe==0.10.8
numpy==1.24.3
Pillow==10.0.1
pyttsx3==2.90

# 새로운 패키지 (Phase 2)
ultralytics==8.0.196        # YOLO 모델
pygame==2.5.2               # 알람 사운드
scipy==1.11.4              # 신호 처리 (필요시)

# 선택적 패키지
# torch==2.0.1              # GPU 가속 (선택사항)
# torchvision==0.15.2       # GPU 가속 (선택사항)
